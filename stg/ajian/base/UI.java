package stg.ajian.base;

import javax.swing.*;

public class UI {

	public UI(Window win) {
		// TODO 自动生成的构造函数存根
		JPanel jp = new JPanel();
		win.add(jp);
		JSplitPane jsp = new JSplitPane();
		win.add(jsp);
	}

}
